import styled from "styled-components";

export const Button = styled.button`
 background-color: var(--white);
 border:var(--border);
 padding: 5px 7px;
 cursor: pointer;
 >svg{
     color: var(--icons);
 }
`